version: '3'

services:
  database:
    restart: always
    image: postgres:latest
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: root

volumes:
  pgdata:
    driver: local